---
import Layout from "../../layouts/Layout.astro";
import PowerList from "../../components/PowerList";
---

<Layout title="Lista de Poderes - Superheroes INC.">
    <section class="powers-page">
        <div class="header">
            <h1 class="page-title">Lista de Poderes</h1>
            <p class="intro-text">
                Consulta rápida de poderes por origen. Los costes en puntos de
                creación incluyen las erratas corregidas y estandarizadas.
                Costes revisados por ultima vez el 14/12/2025.
            </p>
            <a href="/recursos" class="back-link">← Volver a Recursos</a>
        </div>

        <div class="powers-container">
            <PowerList client:load />
        </div>
    </section>
</Layout>

<style>
    .powers-page {
        padding: 2rem 1rem;
        max-width: 1200px;
        margin: 0 auto;
    }

    .header {
        text-align: center;
        margin-bottom: 3rem;
    }

    .page-title {
        font-size: 3rem;
        color: var(--color-secondary);
        text-shadow: 3px 3px 0px var(--color-primary);
        margin-bottom: 1rem;
    }

    .intro-text {
        font-size: 1.2rem;
        max-width: 800px;
        margin: 0 auto 2rem;
    }

    .back-link {
        display: inline-block;
        color: var(--color-secondary);
        font-weight: bold;
        text-decoration: none;
        border-bottom: 2px solid transparent;
        transition: border-color 0.2s;
    }

    .back-link:hover {
        border-bottom-color: var(--color-secondary);
    }

    .powers-container {
        background: white;
        padding: 2rem;
        border: 4px solid var(--color-secondary);
        box-shadow: 8px 8px 0px var(--color-secondary);
    }

    @media (max-width: 600px) {
        .powers-page {
            padding: 1rem;
        }

        .page-title {
            font-size: 2rem;
        }

        .powers-container {
            padding: 1rem;
        }
    }
</style>
