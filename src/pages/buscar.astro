---
import Layout from "../layouts/Layout.astro";
import Search from "../components/Search";
---

<Layout title="Buscar">
    <div class="search-page">
        <header class="page-header">
            <h1>Buscar</h1>
            <p class="subtitle">
                Encuentra contenido en blog, lore, organizaciones y personajes
            </p>
        </header>

        <div class="search-section">
            <Search client:load />
        </div>

        <div class="search-tips">
            <h2>Consejos de búsqueda</h2>
            <ul>
                <li>Usa palabras clave específicas para mejores resultados</li>
                <li>
                    Navega con las flechas ↑↓ y presiona Enter para ir al
                    resultado
                </li>
                <li>Presiona Esc para cerrar los resultados</li>
                <li>La búsqueda incluye títulos, descripciones y alias</li>
            </ul>
        </div>
    </div>
</Layout>

<style>
    .search-page {
        max-width: 800px;
        margin: 0 auto;
        padding: var(--spacing-2xl) var(--spacing-lg);
    }

    .page-header {
        text-align: center;
        margin-bottom: var(--spacing-2xl);
    }

    .page-header h1 {
        font-size: var(--text-5xl);
        font-weight: 800;
        margin-bottom: var(--spacing-md);
        color: var(--color-secondary);
    }

    .subtitle {
        font-size: var(--text-xl);
        color: var(--color-text-light);
    }

    .search-section {
        margin-bottom: var(--spacing-2xl);
    }

    .search-tips {
        background: var(--color-background-alt);
        border-left: 4px solid var(--color-primary);
        padding: var(--spacing-lg);
        border-radius: 4px;
    }

    .search-tips h2 {
        font-size: var(--text-xl);
        margin-bottom: var(--spacing-md);
        color: var(--color-secondary);
    }

    .search-tips ul {
        list-style: none;
        padding: 0;
        margin: 0;
    }

    .search-tips li {
        padding: 0.5rem 0;
        color: var(--color-text);
        position: relative;
        padding-left: 1.5rem;
    }

    .search-tips li::before {
        content: "→";
        position: absolute;
        left: 0;
        color: var(--color-primary);
        font-weight: bold;
    }

    @media (max-width: 768px) {
        .page-header h1 {
            font-size: var(--text-4xl);
        }
    }
</style>
